<%= form_for @sort, :html => {:multipart => true}  do |f| %>
<% if @sort.errors.any? %>
    <div id="error_explanation">
    <h2><%= pluralize(@article.errors.count, "error") %> prohibited
      this sort from being saved:</h2>
    <ul>
    <% @sort.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<p>
<%= f.label :LOGO%><br>
<%= image_tag(@sort.logo_url) %>
<%= f.file_field :logo %>
<%= f.hidden_field :logo_cache  %>
</p>
  <p>
    <%= f.label :名字 %><br>
    <%= f.text_field :name %>
  </p>

    <p>
      <%= f.label :介绍 %><br>
      <%= f.text_area :introduction %>
    </p>

      <p>
        <%= f.submit %>
      </p>
    <% end %>
